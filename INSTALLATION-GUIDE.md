# 📦 دليل التثبيت والتشغيل

## 🎯 الخطوات الصحيحة:

### 1️⃣ حمّل ملفات المشروع
من Claude، حمّل مجلد `react-project` كامل إلى جهازك.
مثلاً في: `~/Downloads/react-project`

### 2️⃣ افتح Terminal وادخل للمجلد:
```bash
cd ~/Downloads/react-project
```

### 3️⃣ تحقق أنك في المجلد الصحيح:
```bash
ls
```
يجب أن تشاهد:
- package.json
- src/
- index.html
- vite.config.js

### 4️⃣ ثبّت المكتبات:
```bash
npm install
```

### 5️⃣ شغّل المشروع:
```bash
npm run dev
```

---

## 🚨 حل المشاكل الشائعة:

### المشكلة: `Missing script: "dev"`
**السبب**: أنت في مجلد خطأ
**الحل**: 
```bash
# تأكد أنك في مجلد المشروع
pwd
# يجب أن يكون الناتج شيء مثل:
# /Users/faytek/Downloads/react-project

# إذا لم تكن في المجلد الصحيح:
cd ~/Downloads/react-project
```

### المشكلة: `command not found: npm`
**الحل**: ثبّت Node.js من https://nodejs.org

### المشكلة: الملفات غير موجودة
**الحل**: حمّل المشروع من Claude مرة أخرى

---

## 🎬 فيديو الخطوات:

```bash
# 1. التحقق من مكانك
pwd

# 2. الذهاب لمجلد المشروع
cd ~/Downloads/react-project

# 3. عرض الملفات
ls -la

# 4. التثبيت
npm install

# 5. التشغيل
npm run dev
```

---

## ✨ بديل سريع (بدون React):

إذا واجهت مشاكل، استخدم النسخة HTML:

1. حمّل ملف: `halal-scanner-final.html`
2. افتحه مباشرة في المتصفح
3. يعمل بدون أي setup!

---

## 📞 لو ما زال عندك مشكلة:

شارك معي:
1. ناتج: `pwd`
2. ناتج: `ls -la`
3. ناتج: `which npm`

وسأساعدك!
